When this skill is invoked, immediately use the Agent tool with the configuration below.
  Do NOT do any analysis yourself first. Call the Agent tool as your very first action.

  subagent_type: general-purpose
  description: "Full codebase architecture audit → ARCHITECTURE_STATE.md"

  prompt:
    You are a ruthless Principal Engineer auditing the "AI Arena Fight" FastAPI backend.
    Your sole deliverable is ARCHITECTURE_STATE.md written to the repo root.
    Do not ask questions. Do not respond in chat. Scan → analyze → write the file → done.

    --- STEP 1: LOAD SPECS (source of truth) ---
    Read these files completely before touching any Python files:
    - CLAUDE.md
    - docs/features/04_persistence_memory.md
    - docs/features/04_implementation_plan.md
    - Any other .md files found under docs/ (glob docs/**/*.md)

    --- STEP 2: SCAN CODEBASE ---
    Glob **/*.py under backend_arena/ and alembic/.
    Read every file completely. For each file record:
    - Lines of code (non-blank)
    - Classes and top-level functions defined
    - External imports
    - What the file actually does

    Also check for existence (do not read, just check exists/missing):
    - creator_dashboard/app.py
    - docs/SYSTEM_ARCHITECTURE.md
    - tests/ directory
    - .env (not .env.example)
    - Dockerfile or docker-compose.yml

    --- STEP 3: SCORE EACH FILE ---
    For every Python file, score 0-100 on three axes against the specs:
    - Completeness: does it implement everything the spec requires?
    - Robustness: error handling, edge cases, data integrity
    - Spec-Alignment: does the logic match the architecture contracts in CLAUDE.md?

    Flag any file scoring below 70 on any axis as "needs refactor".

    --- STEP 4: WRITE ARCHITECTURE_STATE.md ---
    Write to the repo root. Use exactly this structure:

    # ARCHITECTURE_STATE.md
    _Generated: {ISO timestamp} | Total LOC scanned: {n}_

    ## 1. The Blueprint
    2-3 paragraphs. Plain English. Readable by a newcomer in 60 seconds.
    Cover: what it does, who uses it, the Internal Monologue architecture innovation,
    current tech stack (FastAPI + SQLite + SQLAlchemy + multi-LLM routing).

    ## 2. Rock-Solid — Working Components
    Table with columns:
    | File | LOC | Role | Completeness | Robustness | Spec-Align | Status |

    Include all files scoring 70+ on every axis.
    Below the table: 1-2 sentences on what the system can do end-to-end right now.

    ## 3. Tech Debt — Needs Refactor
    For each file scoring below 70 on any axis:

    ### `path/filename.py` — (C:XX R:XX S:XX)
    One-line role description.
    - **Issue:** Specific problem — reference function name or line number if possible.
    - **Missing:** What the spec requires that is absent.

    If zero files need refactor, write: "No critical tech debt identified at this time."

    ## 4. The Pending Pipeline
    ### 4a. Missing Components
    | Component | Required By | Priority |
    |-----------|-------------|----------|
    List every should-exist item not found on disk. Priority: CRITICAL / HIGH / MEDIUM / LOW.

    ### 4b. Next 5 Engineering Actions (Priority Ordered)
    1. [PRIORITY] Action — one-line rationale
    (ordered by impact × urgency)

    ---
    _Generated by /audit skill · AI Arena Fight_

  ---